%div.inst-box
  %header
    = f.label :name, 'Service Name'
    %span.desc
      The name of the service.
  %p
    = f.text_field :name, required: true, maxlength: 255, 
      class: "span10#{service_has_aasm_error_for('name')}"
